_schema-version: '3.1'
ID: mysecapp
description: A simple CAP project.
version: 1.0.0
modules:
  - name: mysecapp-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: mysecapp-db
    provides:
      - name: srv-api
        properties:
          srv-url: '${default-url}'
  - name: mysecapp-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: mysecapp-db
    parameters:
      buildpack: nodejs_buildpack
  - name: app
    type: approuter.nodejs
    path: app
    requires:
      - name: mysecapp_html_repo_runtime
      - name: uaa_mysecapp
      - name: srv-api
        group: destinations
        properties:
          name: srv-api
          url: '~{srv-url}'
          forwardAuthToken: true
    parameters:
      disk-quota: 256M
      memory: 256M
  - name: mysecapp_ui_deployer
    type: com.sap.application.content
    path: .
    requires:
      - name: mysecapp_html_repo_host
        parameters:
          content-target: true
resources:
  - name: mysecapp-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
    properties:
      hdi-service-name: '${service-name}'
  - name: mysecapp_html_repo_runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
  - name: uaa_mysecapp
    type: org.cloudfoundry.managed-service
    parameters:
      path: ./xs-security.json
      service: xsuaa
      service-name: mysecapp-xsuaa-service
      service-plan: application
  - name: mysecapp_html_repo_host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm install --production
        - npx -p @sap/cds-dk cds build --production
